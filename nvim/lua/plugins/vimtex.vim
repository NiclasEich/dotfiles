return {
  "lervag/vimtex",
  ft = { "tex", "plaintex", "latex" },
  init = function()
    -- Example tweaks (uncomment/adjust if you used them):
    -- vim.g.vimtex_view_method = "zathura"  -- or "skim" on macOS
    -- vim.g.vimtex_quickfix_mode = 0
  end,
}

